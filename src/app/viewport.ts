// src/app/viewport.ts
import type { Viewport } from 'next';

export const viewport: Viewport = {
  themeColor: '#00d6d6',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};
