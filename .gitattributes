# === Dados tabulares ===
*.csv text eol=lf
*.tsv text eol=lf
*.json text eol=lf

# === Scripts ===
*.py text eol=lf
*.sh text eol=lf

# === Infraestrutura ===
*.tf text eol=lf
*.tfvars text eol=lf

# === Markdown e docs ===
*.md text eol=lf
*.txt text eol=lf

# === UTF-8 for all text files ===
* text=auto eol=lf

# === Treat CSV as text for diff (melhora a legibilidade em PRs) ===
*.csv diff=csv
