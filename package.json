{
  "name": "observatudo",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "lint": "turbo run lint",
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
